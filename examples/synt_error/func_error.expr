int function SumArray(int[] arr, int size) {
    int total = 0;
    for (int i = 0; i <= size; i++) {  // Ошибка: выход за границы (i <= size)
        total = total + arr[i];
    }
    return total;
}

void function Test() {
    break;  // Ошибка: break вне цикла
    return 1;  // Ошибка: возврат значения из void функции
}

int function Main() {
    int array[3] = [1, 2, 3];
    int sum = SumArray(array, 3.5);  // Ошибка: нецелочисленный размер
    return 0;
}